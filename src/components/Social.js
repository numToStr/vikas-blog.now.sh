import React from "react";
import { Box } from "@material-ui/core";

const Social = () => {
    return (
        <Box display="flex" flexDirection="column" alignItems="center">
            <Box bgcolor="background.paper" height={20} width={20} mb={5} />
            <Box bgcolor="background.paper" height={20} width={20} mb={5} />
            <Box bgcolor="background.paper" height={20} width={20} mb={5} />
            <Box bgcolor="background.paper" height={20} width={20} mb={5} />
        </Box>
    );
};

export default Social;
